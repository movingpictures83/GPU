{
	"answers": [
		"Is a virtual address space accessible both from the host and from the device.",
		"It hides the need for explicit memory transfers between the host and the device.",
		"`cudaMemPrefetchAsync` and `cudaMemAdvise`.",
		"It referes to the host in CUDA API calls.",
		"It makes the memory pages available to the device before it starts using them, thus avoiding page faults.",
		"Only devices with the `cudaDevAttrConcurrentManagedAccess` flag set to non zero may use `cudaMemPrefetchAsync` to prefetch regions to the device."
	]
}
